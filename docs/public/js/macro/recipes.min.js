/* Recipes, By BlackStar, Based on simple-inventory, Ver.0.2.0.alpha.4 */
// 测试版，请勿更新
(()=>{"use strict";const Conditions=Object.freeze({INCLUDES:"includes",EQUALS:"equals"});const defaultOpts={successRate:100,unlock:false,priority:100,condition:Conditions.INCLUDES};setup.Conditions=Conditions;window.Conditions=window.Conditions||Conditions;class Recipe{constructor(id,methods=[],opts=clone(defaultOpts),tags=[]){this.id=id;if(typeof methods!=="object"||typeof opts!=="object"){throw new Error("Invalid recipe definition")}this.methods=methods;Object.assign(this,Object.assign({},defaultOpts,opts));this._tags=tags instanceof Array?tags:typeof tags==="string"?[tags]:[]}get name(){return this.displayName||this.id}set name(val){this.displayName=val}get tags(){return this._tags}hasTag(tag){return this._tags.includes(tag)}hasAllTags(tags){return this._tags.includesAll(tags)}hasAnyTags(tags){return this._tags.includesAny(tags)}validate(ingredients){switch(this.condition){case Conditions.EQUALS:return this.equals(ingredients);case Conditions.INCLUDES:return this.includes(ingredients);default:throw new TypeError(`Invalid condition type: ${this.condition}`)}}calculate(input,index){if(this.ingredients.length<=index){throw new Error(`Unable to access recipe at index ${index}, total number of available crafting methods is ${this.ingredients.length}`)}return Object.entries(this.methods[index]).reduce(((min,[ingredient,require])=>{const available=input[ingredient];return Math.min(min,Math.floor(available/require))}),Infinity)}multiply(servings,index){if(index!==undefined&&this.ingredients.length<=index){throw new Error(`Unable to access recipe at index ${index}, total number of available crafting methods is ${this.ingredients.length}`)}if(typeof servings!=="number"||servings<=0){throw new Error("Invalid number of servings")}const required=[];const processList=index!==undefined?[this.methods[index]]:this.methods;for(const method of processList){const obj={};for(const[ingredient,number]of Object.entries(method)){obj[ingredient]=number*servings}required.push(obj)}return required}includes(ingredients){if(Object.keys(ingredients).length<Object.keys(this.ingredients).length){return undefined}for(const[index,method]of this.methods.entries()){let match=true;for(const ingredient in method){if(!ingredients[ingredient]||ingredients[ingredient]<this.ingredients[ingredient]){match=false;break}}if(match){return index}}return undefined}cook(){if(this.successRate===100){return true}const number=randomInt(1,100);if(this.successRate>=number){return true}return false}equals(ingredients){if(Object.keys(ingredients).length!==Object.keys(this.ingredients).length){return undefined}for(const[index,method]of this.methods.entries()){let match=true;for(const ingredient in method){if(!ingredients[ingredient]||ingredients[ingredient]!==this.ingredients[ingredient]){match=false;break}}if(match){return index}}return undefined}}setup.Recipe=Recipe;window.Recipe=window.Recipe||Recipe})();(()=>{"use strict";const convertToArray=input=>{const array=[];for(const[key,value]of Object.entries(input)){array.push(key,value)}return array};class RecipeBook{static SUCCESS="制作成功";static FAILURE="制作失败";static NOT_FOUND="配方不存在";static NOT_UNLOCKED="配方未解锁";static MISSING_MATERIALS="缺少材料";static UNLOCKED_SUCCESS="配方成功解锁";constructor(ct,recipes=new Map,invs=[]){if(!(recipes instanceof Map)){throw new Error('Expected "recipes" to be an instance of Map')}if(!Array.isArray(invs)){throw new Error('Expected "invs" to be an array')}this.invs=invs;this.ct=ct;this.recipes=recipes;this._recipes=RecipeBook.sort(recipes)}has(id){return this.recipes.has(id)}hasItems(input){const obj=State.getVar(this.ct);for(const[key,required]of Object.entries(input)){const actualCount=obj.count(key);if(actualCount<required){return false}}return true}drop(input){const obj=State.getVar(this.ct);obj.drop(...convertToArray(input))}update(){this._recipes=RecipeBook.sort(this.recipes)}hasAllTags(){const tags=[].slice.call(arguments).flat(Infinity);const result=[];for(const recipe of this._recipes){if(recipe.hasAllTags(tags)){result.push(recipe)}}return result}searchIndex(priority){let low=0;let high=this._recipes.length-1;while(low<=high){const mid=Math.floor((low+high)/2);if(this._recipes[mid].priority===priority){return mid}else if(this._recipes[mid].priority<priority){low=mid+1}else{high=mid-1}}return low}insert(id,methods,opts,tags){if(!Array.isArray(methods)){methods=[methods]}const recipe=this.set(id,methods,opts,tags);const index=this.searchIndex(recipe.priority);this._recipes.splice(index,0,recipe)}set(id,methods,opts,tags){if(!Array.isArray(methods)){methods=[methods]}const recipe=new Recipe(id,methods,opts,tags);this.recipes.set(id,recipe);return recipe}cook(input,servings=1){let[idx,recipe]=this.match(input);let result={id:"",success:false,unlock:false,exist:false,servings:0,message:RecipeBook.NOT_FOUND};if(!recipe.unlock&&typeof input==="string"){result.id=recipe.name;result.success=false;result.unlock=false;result.message=RecipeBook.NOT_UNLOCKED}else if(typeof input==="string"&&idx===undefined){result.id=recipe.name;result.servings=0;result.success=false;result.message=RecipeBook.MISSING_MATERIALS;const required=recipe.multiply(servings);for(const ele of required){if(this.hasItems(ele)){this.drop(ele);result.servings=servings;result.message=RecipeBook.SUCCESS;break}}}else if(idx!==undefined,recipe!==undefined){result.id=recipe.id;result.success=recipe.cook();input.length=0;if(result.success){result.servings=recipe.calculate(input,idx);if(!recipe.unlock){result.message=RecipeBook.UNLOCKED_SUCCESS}else{result.message=RecipeBook.SUCCESS}recipe.unlock=true}else{result.servings=0;result.message=RecipeBook.FAILURE}}return result}cookSave(inv,input,servings=1){const result=this.cook(input,servings);if(result.success){RecipeBook.pickup(inv,result.id,result.servings)}return result}match(input){if(typeof input==="object"&&input!==null){for(const recipe of this._recipes){if(!(recipe instanceof Recipe)){throw new Error(`Invalid Recipe: ${recipe}`)}const idx=recipe.validate(input);if(idx!==undefined){return[idx,recipe]}}}else if(typeof input==="string"){const recipe=this.recipes.get(input);if(recipe===undefined){return undefined}return[undefined,recipe]}else{throw new Error(`Invalid argument: ${input}`)}return undefined}static sort(recipes){const defaultPriority=100;const recipeArr=Array.from(recipes.values());let counts={};recipeArr.forEach((recipe=>{const priority=recipe.priority!==undefined?recipe.priority:defaultPriority;counts[priority]=(counts[priority]||0)+1}));const priorities=Object.keys(counts).map(Number).sort(((a,b)=>a-b));let accumulatedCounts={};let cumulativeSum=0;priorities.forEach((priority=>{accumulatedCounts[priority]=cumulativeSum;cumulativeSum+=counts[priority]}));let arr=Array(recipeArr.length);for(let i=recipeArr.length-1;i>=0;i--){const recipe=recipeArr[i];const priority=recipe.priority!==undefined?recipe.priority:defaultPriority;let position=accumulatedCounts[priority];arr[position]=recipe;accumulatedCounts[priority]++}return arr}static pickup(inv,...item){if(!(inv instanceof Inventory)){throw new Error("Invalid inventory")}return inv.pickup(...item)}}State.variables.crafting_table=State.variables.crafting_table||new Inventory;setup.RecBook=RecipeBook;window.RecBook=window.RecBook||RecipeBook;setup.RecipeBook=setup.RecipeBook||new RecipeBook("$crafting_table");window.RecipeBook=window.RecipeBook||setup.RecipeBook})();(()=>{Macro.add("cook",{tags:null,handler(){if(this.args.length<1){return this.error("Not enough arguments provided. At least two arguments are required")}const items=this.args[0];if(!(typeof items==="object"||typeof items==="string")||items===null){return this.error("Expected an object or a string, but received: "+typeof items)}let result;if(this.args.length>=2){const inv=this.args[1];if(!inv||!(inv instanceof Inventory)){return this.error("Expected an instance of Inventory, but received: "+typeof inv)}result=RecipeBook.cookSave(inv,items,parseInt(this.args[2])||1)}else{result=RecipeBook.cook(items)}WikiVars({result:result},this)}});Macro.add("recipes_with_tag",{handler(){if(this.args.length<1){return this.error("Not enough arguments provided. At least one tag is required")}reusult=RecipeBook.filter(this.args);WikiVars({result:result},this)}})})();
/* End Recipes */